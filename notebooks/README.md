"""
This directory should contain Jupyter notebooks (`.ipynb`) for prototyping,
data exploration, and examples.

Best Practices:
- Number your notebooks if order matters (e.g., `01_exploration.ipynb`)
- Move stable code from notebooks into `src/package_name/` as soon as possible
- Use `jupytext` or check output cells before committing to keep diffs clean
"""
